From 2db9047e9ea01d4a3c7ccd98b4a0ce58a67228df Mon Sep 17 00:00:00 2001
From: Soff <soffchen@gmail.com>
Date: Thu, 27 Feb 2020 22:58:39 +0800
Subject: [PATCH 20/39] Add r2s support, add Comfast 726B(8821CU) support

---
 package/base-files/files/etc/hotplug.d/usb/31-usb_wifi | 8 +-------
 1 file changed, 1 insertion(+), 7 deletions(-)

diff --git a/package/base-files/files/etc/hotplug.d/usb/31-usb_wifi b/package/base-files/files/etc/hotplug.d/usb/31-usb_wifi
index 19910823a8..b10ef09ef1 100644
--- a/package/base-files/files/etc/hotplug.d/usb/31-usb_wifi
+++ b/package/base-files/files/etc/hotplug.d/usb/31-usb_wifi
@@ -2,14 +2,8 @@
 
 #logger ACTION=$ACTION DEVPATH=$DEVPATH SUBSYSTEM=$SUBSYSTEM DEVTYPE=$DEVTYPE PRODUCT=$PRODUCT MODALIAS=$MODALIAS
 
-PLATFORM='sun8iw7p1|sun50iw2p1'
-if ! grep -E $PLATFORM /sys/class/sunxi_info/sys_info -q; then
-    logger "only support $PLATFORM. exiting..."
-    exit 0
-fi
-
 FE_DIR=/root/.friendlyelec/
-if [ "${DEVTYPE}" = usb_device -a "${PRODUCT}" = "bda/c811/200" ]; then
+if [ "${DEVTYPE}" = usb_device ] && { [ "${PRODUCT}" = "bda/c811/200" ] || [ "${PRODUCT}" = "bda/c820/200" ] ;}; then
     MODULE=rtl8821cu
     TARGET=wlan${MODULE}
 
-- 
2.28.0.windows.1

